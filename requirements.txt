# Runtime dependencies only — dev tools (mypy, ruff, pytest) live in requirements-dev.txt.
# Versions pinned from container (2026-06-11).
# ~=X.Y allows patch/minor updates within the same major (e.g. ~=1.3 → >=1.3,==1.*).
# torch is installed separately in the Dockerfile with --index-url cpu-only to avoid
# pulling 3.5 GB of CUDA/nvidia/triton binaries that are never used (CPU inference only).
# matplotlib: used for headless PNG export (PDF charts). kaleido removed — it needed
# Electron system libs absent from python:3.11-slim and failed silently in the container.

langchain~=1.3
langchain-google-genai~=4.2
langchain-openai~=1.3
langchain-community~=0.4
langchain-huggingface~=1.2
langchain-postgres~=0.0
langgraph~=1.2
sqlalchemy~=2.0
psycopg2-binary~=2.9
pgvector~=0.3
pandas~=3.0
sentence-transformers~=5.5
ddgs~=9.14
plotly~=6.8
matplotlib~=3.9
fpdf2~=2.8
streamlit~=1.58
python-dotenv~=1.2
pydantic~=2.13
pydantic-settings~=2.14
